{
  "name": "argumentsof",
  "version": "1.0.0",
  "description": "Npm module which parses arguments of any callables.",
  "main": "argumentsOf.js",
  "scripts": {
    "preversion": "npm test -- --coverage",
    "prepublish": "npm test -- --coverage",
    "test": "jest"
  },
  "keywords": ["parse", "arguments", "callable", "function", "dependency injection", "di"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.4.3",
    "@babel/preset-env": "^7.4.3",
    "babel-jest": "^24.7.1",
    "jest": "^24.7.1"
  },
  "jest": {
    "coverageThreshold": {
      "global": {
        "branches": 100,
        "functions": 100,
        "lines": 100,
        "statements": 100
      }
    }
  }
}
